# test_terraform

repositorio creado para automatizar despliegues de terraform.
solo se implementara infraestructura al hacer un push o merge en la rama main

objetivo secundario:
lograr que al realizar un commit desde otra rama se haga el terrraform init y plan, para que al realizar la aprobación del pull request, se apliquen los cambios en la infra 



